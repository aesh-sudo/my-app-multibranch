pipeline {
    agent any
    
    stages {
        stage('main branch') {
            steps {
                echo 'Branch name: main'
                echo 'Build number: ${BUILD_NUMBER}'
            }
        }
    }
}
