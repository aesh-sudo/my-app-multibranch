pipeline {
    agent any

    stages {
        stage('feature/login branch') {
            steps {
                echo 'Branch name: feature/login'
                echo "Build number: ${BUILD_NUMBER}"
            }
        }
    }
}

