pipeline {
    agent any
    
    stages {
        stage('feature/cart branch') {
            steps {
                echo 'Branch name: feature/cart'
                echo "Build number: ${BUILD_NUMBER}"
            }
        }
    }
}
